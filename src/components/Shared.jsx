import React,{useEffect} from 'react';
import {X,GraduationCap,ArrowUpRight,Loader2} from 'lucide-react';
export function Brand({settings,onClick}){return <button className="brand" onClick={onClick} aria-label="Sure60 home">{settings?.logo_url?<img src={settings.logo_url} alt="Sure60"/>:<span className="brand-icon"><GraduationCap size={27}/></span>}<span>sure<span className="orange">60</span><small>LEARN. BELIEVE. ACHIEVE.</small></span></button>}
export function Modal({title,children,onClose,wide=false}){useEffect(()=>{const prev=document.activeElement;const old=document.body.style.overflow;document.body.style.overflow='hidden';const node=document.querySelector('.modal');node?.focus();const handler=e=>{if(e.key==='Escape')onClose();if(e.key==='Tab'){const nodes=node?.querySelectorAll('button,input,select,textarea,a[href],[tabindex="0"]');if(!nodes?.length)return;const first=nodes[0],last=nodes[nodes.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}};document.addEventListener('keydown',handler);return()=>{document.body.style.overflow=old;document.removeEventListener('keydown',handler);prev?.focus()}},[onClose]);return <div className="modal-backdrop" onClick={e=>e.target===e.currentTarget&&onClose()}><section className={`modal ${wide?'wide':''}`} role="dialog" aria-modal="true" aria-label={title} tabIndex={-1}><header><h2>{title}</h2><button className="icon-button" onClick={onClose} aria-label="Close dialog"><X/></button></header>{children}</section></div>}
export function Button({children,loading,className='',...props}){return <button className={`button ${className}`} disabled={loading||props.disabled} {...props}>{loading?<Loader2 className="spin" size={18}/>:null}{children}</button>}
export function SectionTitle({label,title,subtitle,action,onAction}){return <div className="section-heading"><div><span className="eyebrow">{label}</span><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div>{action&&<button className="text-button" onClick={onAction}>{action}<ArrowUpRight size={18}/></button>}</div>}
export function Empty({title,description}){return <div className="empty"><GraduationCap size={38}/><h3>{title}</h3><p>{description}</p></div>}
